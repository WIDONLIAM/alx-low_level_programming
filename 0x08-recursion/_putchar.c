#include "main.h"
#include <unist.h>

/**
 *
 * _putchar - writes the c character to (stdout)
 * @c: shows the character to print
 *
 * Return: success (1)
 *
 */

int _putcgar(char c)
{
	return (write(1, &c, 1));

}
