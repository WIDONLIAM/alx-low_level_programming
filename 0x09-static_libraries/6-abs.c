#include "main.h"

/**
 * _abs - Will be computeing the  positive value of an integer
 * @n: Is the integer to check
 *
 * Return: positive value of the integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
