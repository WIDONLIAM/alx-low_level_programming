#include "main.h"

/**
 * _isdigit - Will be checking for digits (0 through 9)
 * @i: Is the integer to be checked
 *
 * Return: (1) if i is a digit, (0) otherwise
 */

int _isdigit(int i)
{
	return (i >= '0' && i <= '9');
}
