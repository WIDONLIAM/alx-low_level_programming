#include "main.h"

/**
 * print_array - a function that prints n elements of an array
 * @a: array name
 * @n: is the number of elements of  the array that are going to be printed
 * Return: a and n imputs
 */

void print_array(int *a, int n)
{
	printf("%d", a[i]);

		if (i == (n - 1)
			{
				printf("%d", a[n - 1]);
			}
				printf("\n");
}
